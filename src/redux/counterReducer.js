const intialState = {
    number: 0,
}
const counterReduce = (state = intialState, action) => {
    switch (action.type) {
        case "increment_by_1":

            break;

        default:
            break;
    }
    return state;
}
export default counterReduce;